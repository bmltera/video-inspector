# Build resources

Electron Builder automatically uses `build/icon.ico` for Windows builds when it exists.

To customize the installer/app icon later, add a valid Windows `.ico` file here:

```text
build/icon.ico
```

Recommended icon sizes inside the ICO: 16, 32, 48, 64, 128, and 256 px.
